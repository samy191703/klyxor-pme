apiVersion: v1
kind: Service
metadata:
  name: clm-app
  namespace: NAMESPACE_PLACEHOLDER
spec:
  selector:
    app: clm-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
