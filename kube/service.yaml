apiVersion: v1
kind: Service
metadata:
  name: clm-front-admin
  namespace: NAMESPACE_PLACEHOLDER
spec:
  selector:
    app: clm-front-admin
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
